var solution = 42;
var tries = 0;

while (true) {
	tries++;
	var guess = input("Guess a number: ");

	if (solution == guess) {
		log("You won!");
		log("You needed a total of " + tries + " tries to guess the right number.");
		break;
	}
	else if (guess > solution) {
		log("Your Guessed to high");
	} else {
		log("Your Guessed to low");
	}
	
	log("\n--- Next try! ---\n\n")
}
