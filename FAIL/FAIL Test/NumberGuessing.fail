var solution = 42;
var tries = 0;

while (true) {
	tries++;
	var guess = input("Guess a number: ");

	if (solution == guess) {
		|> "You won!");
		|> "You needed a total of " + tries + " tries to guess the right number.");
		break;
	}
	else {
		|> "This is not the right number!");
	}
}