using FAIL.Language_Integration;

namespace FAIL.ElementTree.IntegratedFunctions;
internal abstract class IntegratedFunction : AST
{
    public IntegratedFunction(Token? token = null) : base(token) { }
}
